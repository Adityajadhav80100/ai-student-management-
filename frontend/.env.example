# Frontend environment variables (rename to .env or set in your hosting provider)
# Netlify builds require the variable to be defined in Netlify UI or injected via .env.production.local.
VITE_API_URL=https://ai-student-management-dnz1.onrender.com/api
